# Core / perception (OpenCV notebook)
# numpy pinned <2 because nes-py / gym-super-mario-bros require the numpy 1.x ABI.
# opencv pinned <4.12 because 4.12+ declares numpy>=2 (conflicts with the pin above).
numpy<2
opencv-python-headless>=4.9,<4.12
matplotlib
jupyterlab
pandas
scipy
seaborn

# Reinforcement learning env (Phase 1 — NES Super Mario Bros)
# gym pinned 0.25.2: last release with the old 4-tuple step API that nes-py uses.
gym==0.25.2
nes-py==8.2.1
gym-super-mario-bros==7.4.0
imageio[ffmpeg]

# Training (Phase 2 — PPO baseline)
# SB3 speaks Gymnasium; shimmy bridges the old-gym Mario env to Gymnasium.
stable-baselines3==2.7.1
# RecurrentPPO (CnnLstm / MultiInputLstm) lives in sb3-contrib — the meta-RL path.
sb3-contrib==2.7.0
gymnasium==1.1.1
shimmy==2.0.0
torch==2.8.0
tensorboard

# Phase 7 — Atari suite (ALE). Second adapter family behind the GameEnv
# interface. ale-py 0.11 bundles the ROMs and is happy with the numpy<2 pin.
ale-py==0.11.2

# Phase 9 — ARC-AGI-3 (ARC Prize 2026 interactive track). The north-star target
# (see docs/arc-agi-3.md). Needs Python ≥3.10 (hence the 3.13 bump) and coexists
# with the numpy<2 mario pin. Arcade() fetches an anon key + games online.
arc-agi==0.9.9
